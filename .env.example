# Copie este arquivo para .env e preencha os valores

# Token da API TriboPay (NÃO COMITAR EM REPO PÚBLICO)
TRIBOPAY_API_TOKEN=

# (Opcional) Hashes padrão
DEFAULT_OFFER_HASH=ugg3tvujyf
DEFAULT_PRODUCT_HASH=5crbkjmnrj

# (Opcional) URL de postback absoluta
# Se não informar, em produção (Vercel) será montado via VERCEL_URL
# POSTBACK_URL=https://SEU-PROJETO.vercel.app/api/webhook
